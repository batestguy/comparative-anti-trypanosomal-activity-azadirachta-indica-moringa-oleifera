---
title: "Comparative Anti-Trypanosomal Activity of Azadirachta indica and Moringa oleifera against Trypanosoma congolense and T. evansi — Statistical Report"
format:
  docx:
    toc: true
    number-sections: true
    reference-doc: reference-doc.docx
    lof: true
    lot: true
  html:
    toc: true
    number-sections: true
    embed-resources: true
execute:
  echo: false
  warning: false
  message: false
---

```{r}
#| label: setup
library(readr)
library(dplyr)
library(knitr)
tab_dir <- file.path("..", "results", "tables")
fig_dir <- file.path("..", "results", "figures")
# Display names for tables: exact code names stay in Methods (Section 4 table below);
# everywhere else tables carry proper names (underscores expanded, acronyms kept).
proper <- function(nm) {
  nm <- gsub("_", " ", nm)
  nm <- tools::toTitleCase(nm)
  nm <- gsub("\\bTti\\b", "TTI", nm)
  nm <- gsub("\\bAuc\\b", "AUC", nm)
  nm <- gsub("\\bPcv\\b", "PCV", nm)
  nm <- gsub("\\bLoo\\b", "LOO", nm)
  nm <- gsub("^N ", "n ", nm)
  nm
}
k <- function(f, digits = 3) {
  d <- read_csv(file.path(tab_dir, f), show_col_types = FALSE)
  names(d) <- proper(names(d))
  kable(d, digits = digits)
}
# Slim display for contrast tables: split the Dose | Parasite label into two
# narrow columns and combine the HL estimate with its CI, so wide tables fit
# a portrait page without wrapping.
fmt_contrast <- function(f) {
  d <- read_csv(file.path(tab_dir, f), show_col_types = FALSE)
  d$hl <- sprintf("%.2f [%.2f, %.2f]", d$hl_est, d$hl_l, d$hl_u)
  # Short row labels: parenthetical detail lives in the caption, not the cell.
  d$contrast <- sub(" \\(.*\\)$", "", d$contrast)
  # Name columns by identity (never by position: merged tables may lack p_holm).
  disp <- c(endpoint = "Endpoint", contrast = "Contrast", Dose = "Dose",
            Parasite = "Parasite", n_pairs = "n", W = "W", p = "p",
            p_holm = "p (Holm)", hl = "HL [95% CI]", r_rb = "r")
  if (all(grepl(" \\| ", d$contrast))) {
    # Dose tables: split Dose | Parasite into two narrow columns.
    parts <- strsplit(d$contrast, " \\| ")
    d$Dose <- vapply(parts, `[`, "", 1)
    d$Parasite <- vapply(parts, `[`, "", 2)
    cols <- c("Dose", "Parasite", "n_pairs", "W", "p", "p_holm", "hl", "r_rb")
  } else {
    # Merged tables: Endpoint first, one header row, no repeats.
    cols <- c("endpoint", "contrast", "n_pairs", "W", "p", "p_holm", "hl", "r_rb")
  }
  cols <- cols[cols %in% names(d)]
  out <- d[, cols]
  names(out) <- disp[names(out)]
  kable(out, digits = 4)
}
```

## Abstract

Methanolic extracts of *Azadirachta indica* and *Moringa oleifera* (leaves,
stem bark, roots, seeds) were tested against *Trypanosoma congolense* and
*T. evansi* in motility assays and in albino mice (N = 16 plant × part ×
parasite blocks; group means of n = 3 mice). Both parasites responded in a
monotonic, concentration-dependent manner. *T. evansi* responded
significantly (parasitaemia Friedman p = 0.006; 100, 10 and 0.5 mg/ml each
superior to control, Holm 0.039); *T. congolense* responded suggestively
(Friedman p = 0.074; 100 mg/ml Holm 0.071). At 100 mg/ml, mean peak
parasitaemia was zero with no mortality in either parasite, against peak
LEV 5 with universal mortality in Negative Controls. The two plants were
indistinguishable (paired p = 1.0; HL 0.3, 95% CI −14.7 to 20.5). Inference
is block-level and rank-based throughout, and all headline claims withstood
leave-one-block-out sensitivity analysis.

## 1. Aim and structure of this report

The aim of the study is to compare the anti-trypanosomal activity of
methanolic extracts of *A. indica* and *M. oleifera* (leaves, stem bark,
roots, seeds) against *T. congolense* and *T. evansi* (thesis Section 1.8.1). This
report presents the statistical analysis for specific objectives ii–v
(Section 1.8.2): phytochemical characterisation (Section 5.2), the in-vitro motility assay
(Section 5.3), in-vivo infectivity (Section 5.4) and the superiority assessment (Section 5.5),
followed by diagnostics (Section 6), an integrative discussion against the recent
literature (Section 7) and limitations (Section 8). Objective i (extraction) is a
laboratory procedure without recorded replicates and carries no statistical
component (Section 5.1).

## 2. Design and analytical approach

The source data are group means of three mice per group. Consequently, each
treatment group within a plant × part × parasite block is a single
observation, and the block (N = 16; eight per plant and per parasite) is the
unit of analysis. Plant effects are estimated from eight part × parasite
pairs; parasite effects from eight plant × part pairs. Controls are reported
as labelled in the source, their identities and doses being unstated, so the
Positive Control functions as an assay-validity reference rather than a
comparator. With at most eight pairs per test, the design has low power by
construction; effect sizes with confidence intervals are therefore reported
throughout and p-values treated as supplementary.

## 3. Endpoints

The primary parasitaemia endpoint is the death-censored AUC (trapezoidal sum
over observed days), interpreted strictly alongside mortality; peak LEV,
clearance day and day-40 status are supporting. Day-40 LEV alone is
uninformative (no group positive at day 40 against ~30% positivity at days
9–14). Motility is summarised as time-to-immobilisation, t120 motility and
mean proportion motile (source key: `+ Active`, `− No motility`; the two
`+-` cells set to NA). Weight and PCV enter as Post-minus-Pre differences
with literature-based anaemia and weight-loss flags. LEV readings above the
0–5 scale (42 cells) were capped at 5 with raw values preserved and flagged;
missing in-vivo values were taken as deaths (an adopted convention, never
imputed).

## 4. Statistical methods

All inference is rank-based: Friedman tests blocked by block for ordered dose
trends with pooled Spearman correlations on log10 dose, and paired Wilcoxon
signed-rank tests for dose-versus-control post-hoc families (Holm-adjusted
per parasite) and for plant and parasite contrasts, each accompanied by the
matched-pairs rank-biserial effect (|r| ≥ 0.11/0.28/0.43 as small/medium/
large) and the Hodges–Lehmann median difference with 95% confidence
interval. Exact p-values are used wherever the data permit; where tied ranks
or zero differences occur, the continuity-corrected normal approximation is
substituted and the substitution recorded per contrast (Section 6). Per-block trend
tests at n = 1 lack power, so only the blocked tests are inferential.
Computations are in `analysis/R/02_analysis.R`; no machine learning was used.
Analysis variables are specified exactly as named in code and data files in
Table 1; Results and Discussion use proper display names.

```{r}
#| label: tbl-vars
#| tbl-cap: Analysis variables as specified in code and data files.
kable(data.frame(
  Variable = c("`auc`", "`peak_lev`", "`clear_day`", "`day40_lev`",
               "`tti`", "`mot120`", "`mean_motile`", "`delta`", "`present`",
               "`concentration_mgml`"),
  Description = c("Death-censored trapezoidal AUC over observed days",
                  "Maximum LEV within group", "Last day with LEV > 0",
                  "LEV at day 40 (0 or missing)", "First time with motility 0",
                  "Motility at 120 min", "Mean proportion motile over 0-120 min",
                  "Post-Infection minus Pre-Infection (weight, PCV)",
                  "Phytochemical presence (TRUE/FALSE)",
                  "Dose in mg/ml (NA for controls)"),
  File = c(rep("`endpoints_parasitemia.csv`", 4),
           rep("`endpoints_motility.csv`", 3),
           "`endpoints_weight.csv`, `endpoints_pcv.csv`",
           "`phytochemical.csv`", "`motility.csv`, `parasitemia.csv`"),
  check.names = FALSE))
```

## 5.1. Objective i — methanolic extraction

No yields or replicates were recorded for the extraction step, so objective i
contributes no data to the analysis and is noted here only for completeness.

## 5.2. Objective ii — phytochemical contents

The screening panel resolved eight of twelve compound classes in at least one
extract, distributed along a part axis rather than a plant axis. Leaves were
the richest fraction in both species (6 of 12 classes each), stem bark and
roots the sparsest (3–4), and seeds intermediate (4–5; @tbl-phyto, @fig-rich).
Saponins and carbohydrates were ubiquitous (8 of 8 combinations) while
anthraquinones, phlobatannins, oxalates and phytates were uniformly absent,
so neither group discriminates the plants; tannins showed the sharpest part
pattern, present everywhere except seeds in both species (@fig-phyto-hm). The
few differentiating classes were part-specific rather than species-specific:
steroids (only *A. indica* leaves and seeds), terpenoids (only *M. oleifera*
stem bark), and alkaloids, flavonoids and glucosides in asymmetric but
overlapping distributions — the two leaf profiles share six classes apiece
with only the steroid/glucoside entries swapped. With eight combinations,
these data admit description only; compound–efficacy overlays are tabulated
without tests. The substantive point for Section 7 is the extent of chemical overlap
between the species, which leaves little phytochemical basis for expecting
divergent efficacy.

```{r}
#| label: tbl-phyto
#| tbl-cap: Phytochemical richness per plant and part (descriptive).
k("phytochemical_richness.csv", 0)
```

![Richness: classes present of 12](`r file.path(fig_dir, "explore_richness_bar.png")`){#fig-rich}

![Phytochemical presence by plant and part](`r file.path(fig_dir, "phytochemical_heatmap.png")`){#fig-phyto-hm}

## 5.3. Objective iii — in-vitro motility

Both parasites were immobilised in strict concentration order. Mean motility
curves fan out cleanly by dose (@fig-curves): 100 mg/ml collapses motility
within 15–30 minutes in both parasites, each lower dose traces a
progressively slower decline, and Negative Controls remain motile throughout.
Median time-to-immobilisation lengthened monotonically from 15 minutes at
100 mg/ml (uncensored in all blocks) to fully censored controls, with t120
motility rising from zero to 0.67 (*congolense*) and 1.0 (*evansi*) in the
controls (@tbl-tti; @fig-tti). The Positive Controls behaved as a reference
trypanocide should — motile at t = 0, immobile thereafter in all sixteen
tables — which validates the assay and establishes the benchmark that the
top extract dose reproduces. Formally, the blocked trend was highly
significant in both parasites (@tbl-mot-dose; *congolense* Friedman
Q = 27.75, p < 0.001, ρ = −0.70; *evansi* Q = 30.32, p < 0.001, ρ = −0.88).
Post-hoc contrasts against the control (@tbl-mot-posthoc) were uniformly
large at the top doses (rank-biserial −1; HL reductions of −0.83 and −0.89
at 100 mg/ml) but, with eight blocks and Holm correction, stopped just short
of significance (0.065 and 0.052; 10 mg/ml 0.085 and 0.052), and lower doses
were indistinguishable from control — several low-dose pairs matched it
exactly. The in-vitro conclusion is therefore a significant ordered trend
with large but multiplicity-limited pairwise effects, concentrated at
100 mg/ml.

```{r}
#| label: tbl-tti
#| tbl-cap: TTI and t120 summaries per dose (medians; censored groups never immobilise).
k("explore_tti.csv")
```

![Time to immobilisation by dose (black = median; x = censored)](`r file.path(fig_dir, "explore_tti_dose.png")`){#fig-tti}

![Mean proportion motile over time, by dose](`r file.path(fig_dir, "motility_summary_curves.png")`){#fig-curves}

```{r}
#| label: tbl-mot-dose
#| tbl-cap: Motility dose-response on mean proportion motile (Friedman blocked by block; Spearman on log10 dose).
k("dose_response_motility.csv")
```

```{r}
#| label: tbl-mot-posthoc
#| tbl-cap: Post-hoc motility contrasts vs Negative Control (paired Wilcoxon; Holm per parasite).
fmt_contrast("dose_vs_nc_motility.csv")
```

## 5.4. Objective iv — in-vivo infectivity

### Parasitaemia

The in-vivo gradient reproduced the dish. Mean peak parasitaemia declined
monotonically with dose to exactly zero at 100 mg/ml in both parasites with
no deaths, against peak LEV 5 with universal mortality in the Negative
Controls (@tbl-peak; @fig-peak); mortality, clearance time and day-40
clearance all tracked the same ordering (@fig-mort), and trajectories peaked
near days 9–14 before resolving in survivors (@fig-para-traj). On cumulative
burden the *T. evansi* trend was significant (@tbl-dose: Friedman Q = 14.51,
p = 0.006, ρ = −0.51) with 100, 10 and 0.5 mg/ml each superior to control in
all eight blocks (W = 0, exact p = 0.008, Holm 0.039; HL ≈ −50/−50/−44
AUC-days), while 0.01 and 0.005 mg/ml lost significance on correction (Holm
0.078) despite strongly negative estimates. *T. congolense* showed the same
direction with weaker evidence (Friedman p = 0.074; 100 mg/ml Holm 0.071,
HL −29.9 [−41.0, −20.9]; lower doses Holm ≥ 0.32; @tbl-para-posthoc,
@fig-auc). Overall burdens did not differ between parasites (paired p = 0.46,
HL 6.0 [−10.7, 21.1]; @tbl-other): the species differ in dose sensitivity,
not in the burden the model produces.

```{r}
#| label: tbl-peak
#| tbl-cap: Peak, clearance and mortality per dose (means/medians over 8 blocks).
k("explore_peak_mortality.csv")
```

![Peak LEV by dose (black = mean)](`r file.path(fig_dir, "explore_peak_dose.png")`){#fig-peak}

![Groups with any death, by dose](`r file.path(fig_dir, "explore_mortality.png")`){#fig-mort}

```{r}
#| label: tbl-dose
#| tbl-cap: Parasitaemia dose-response on AUC (Friedman blocked by block; Spearman on log10 dose).
k("dose_response.csv")
```

```{r}
#| label: tbl-para-posthoc
#| tbl-cap: Post-hoc parasitaemia contrasts vs Negative Control on AUC (paired Wilcoxon; Holm per parasite).
fmt_contrast("dose_vs_nc_auc.csv")
```

![Mean AUC vs log10(concentration)](`r file.path(fig_dir, "auc_dose_response.png")`){#fig-auc}

![Parasitaemia trajectories by block](`r file.path(fig_dir, "parasitemia_trajectories_faceted.png")`){#fig-para-traj}

### Body weight and PCV

Neither measure responded to treatment. Mean trajectories across the three
timepoints were flat and overlapping (@fig-wp-traj), deltas centred near
zero (@fig-wt, @fig-pcv), and no plant or parasite contrast approached
significance (@tbl-other; all p ≥ 0.15 with intervals spanning zero). The
informative signal is instead where the flags fell (@tbl-flags): deaths,
severe weight loss and anaemia concentrated almost entirely in *T.
congolense* blocks of both plants, with *T. evansi* groups largely unflagged.
Two inferences follow. First, the parasitaemia effects above are not
confounded by differential toxicity — treated groups gained no weight or PCV
advantage because there was no toxicity gradient to begin with. Second, *T.
congolense* is confirmed as the more pathogenic organism in this model,
independently of extract.

```{r}
#| label: tbl-flags
#| tbl-cap: Toxicity/anaemia flag counts (descriptive).
k("explore_toxicity_flags.csv", 0)
```

![Mean weight and PCV across timepoints](`r file.path(fig_dir, "explore_weight_pcv_traj.png")`){#fig-wp-traj}

```{r}
#| label: tbl-other
#| tbl-cap: Parasite, weight and PCV contrasts (paired Wilcoxon; 8 pairs each).
fmt_contrast("other_contrasts.csv")
```

![Body-weight change Post−Pre (g)](`r file.path(fig_dir, "weight_delta_boxplot.png")`){#fig-wt}

![PCV change Post−Pre (pp)](`r file.path(fig_dir, "pcv_delta_boxplot.png")`){#fig-pcv}

## 5.5. Objective v — comparative superiority

The eight paired differences span zero with no structure: three favour *M.
oleifera* (to −21.5 AUC-days), four favour *A. indica* (to +18.1), one is an
exact tie, and neither part nor parasite predicts the sign (@fig-forest;
@fig-paired). The paired test returns the null almost exactly (W = 14,
exact p = 1.0; HL 0.3, 95% CI −14.7 to 20.5; rank-biserial −0.14), and the
motility cross-check concurs (p = 0.15; @tbl-sup). Because the interval
covers meaningful effects in both directions, the data neither establish nor
exclude a modest plant difference — but as a superiority claim for either
agent, the answer is negative, and it is insensitive to any single block
(Section 6).

```{r}
#| label: tbl-sup
#| tbl-cap: Plant superiority contrasts (paired Wilcoxon; 8 pairs each).
fmt_contrast("superiority_combined.csv")
```

![Paired AUC difference per block with overall HL estimate](`r file.path(fig_dir, "plant_forest.png")`){#fig-forest}

![Paired block-mean AUC scatter](`r file.path(fig_dir, "plant_paired_auc.png")`){#fig-paired}

## 6. Diagnostics, sensitivity and refits

Contrasts with tied ranks or zero differences report `n_nonzero`, `n_ties`
and the p-value method used (exact only when clean; continuity-corrected
normal approximation otherwise); refitting on this basis left no conclusion
changed. Paired differences were inspected for the symmetry the signed-rank
test assumes (@fig-sym): *evansi* differences form a pure location shift,
while the superiority differences are symmetric about zero, consistent with
its null. Leave-one-block-out re-analysis (@tbl-loo) found no conclusion
dependent on any single block. Completeness is asserted structurally at
build.

```{r}
#| label: tbl-loo
#| tbl-cap: Leave-one-block-out sensitivity (stable = no single block flips the conclusion).
k("sensitivity_loo.csv")
```

![Paired differences: symmetry check (red = median)](`r file.path(fig_dir, "diagnostic_paired_diffs.png")`){#fig-sym}

## 7. Discussion

The three assay layers converge on one finding — both extracts are genuinely
and dose-dependently trypanocidal — and each layer has a direct precedent in
the recent literature. The in-vitro ordering (TTI of 15 minutes at the top
dose lengthening monotonically to fully censored controls; @fig-tti,
@fig-curves) sits squarely in the published range: *Acacia* stem-bark
extracts immobilised *T. congolense* within 15 minutes at their top doses
(Ogbole et al. 2021), *Ranunculus* extracts and anemonin within 5–30 minutes
(Sirak et al. 2024), and *A. indica* fractions within the hour (Tauheed et
al. 2021). Notably, the Ogbole design is a near twin of the present assay —
identical +/− scoring over 0–120 minutes, the same threshold behaviour with
top doses matching the Berenil control and the lowest dose equalling the
negative control — so the threshold pattern here replicates an established
signature rather than standing alone. One caveat travels with this literature
and applies here: Sirak et al. showed that in-vitro immobilisation at low
doses need not mean death, with parasites recovering infectivity. The present
TTI gradient is therefore read as a kill proxy corroborated by the infectivity
outcomes (peaks and mortality), not as proof of death at every dose.

In-vivo, the dose-dependent suppression of peaks and burden with zero
mortality at the top dose matches the direction reported for *A. indica*
stem bark (Wanzala et al. 2017; Mbaya et al. 2010), *A. indica* leaves (Okoh
2018; Ngure et al. 2009) and *M. oleifera* seed and leaves (Ujah et al. 2020;
Datti et al. 2020), including their shared observation that crude extracts
suppress rather than sterilise at lower doses. Two contrasts with that
literature deserve emphasis. First, the effective threshold here — full
suppression at the top in-vitro-style dose with *T. evansi* yielding from
0.5 mg/ml — is strong for crude methanolic extracts, most of which in the
cited studies reduced but did not clear parasitaemia; whether that reflects
genuine potency or the concentration-based (rather than mg/kg) dosing of
this design is a question for the thesis methods, not this analysis. Second,
Rabe et al. (2024) found ethanolic *A. indica* markedly superior to *M.
oleifera* against a diminazene-resistant *T. brucei* strain, whereas the
present comparison across four parts and two parasites finds no difference;
the reconciliation is context — single leaf extracts against a resistant
strain versus broad part × parasite panels — and it cautions against
generalising either result beyond its design.

The phytochemical picture is consistent with the pharmacology literature
without overreaching it. The classes present here — alkaloids, flavonoids,
saponins and tannins — are precisely those repeatedly tied to trypanocidal
activity (Ujah et al. 2020; Ogbole et al. 2021), and the part-axis variation
(steroids only in *A. indica* leaves/seeds, terpenoids only in *M. oleifera*
stem bark) offers natural fractionation targets. The mechanistic hypothesis
with the most direct support is trypanosome alternative oxidase inhibition,
for which *A. indica* fractions already show dose-dependent motility
suppression coupled to enzyme inhibition (Tauheed et al. 2021); testing the
present extracts in that assay would be the logical next step. The weight and
PCV nulls diverge from studies reporting PCV recovery under treatment
(Datti et al. 2020; Ujah et al. 2020; Tauheed et al. 2021), but those studies
used animal-level data with identified controls and larger groups, whereas
the present group-mean design with eight blocks cannot resolve modest
haematological effects — absence of evidence here, not evidence of absence,
and the flag clustering still independently ranks *T. congolense* the more
pathogenic parasite, in line with the comparative literature. Methodologically,
the Sirak-style ANOVA+Tukey pipeline used across much of this literature
assumes normality and independence that group-mean, ordinal, repeated
observations violate; the block-level rank analysis here answers the same
questions without those assumptions, at the accepted cost of power. Taken
together, the supported position is narrow and firm: both extracts are active
in a dose-dependent manner with the top dose fully suppressive, *T. evansi*
the more sensitive parasite, and neither plant demonstrably superior —
pending animal-level confirmatory work with identified controls, a refined
0.5–10 mg/ml grid around the *congolense* threshold, and fractionation
toward the compound level.

## 8. Limitations

Group means of three mice erase within-group variance, so inference rests on
sixteen blocks; intervals are wide and description carries the weight.
Control identities and doses are unknown, restricting the Positive Control
to a validity reference. Death-censored AUC flatters early-death groups and
is therefore always read with mortality. The NA-equals-death convention
awaits confirmation against animal records. All results are exploratory, not
confirmatory.

## 9. References

- Barrett, M.P. (2025) — on toxicity and resistance limits of current
  trypanocides; cited via Abdullahi et al. (2026).
- Abdullahi, R.I. et al. (2026). Phytochemical profiling and in-vitro
  antitrypanosomal efficacy of *Combretum glutinosum* leaf extract. *FJS*.
- Adams, L. et al. (2024). Antitrypanosomal activities of *Anopyxis
  klaineana* stem bark extracts. *Heliyon* 10: e28025.
- Cliff, N. (1993, 1996); Vargha, A. & Delaney, H.D. (2000). *J. Educ.
  Behav. Stat.* 25(2): 101–133.
- Conover, W.J. (1999). *Practical Nonparametric Statistics* (3rd ed.). Wiley.
- Datti, Y. et al. (2020). In-vivo antitrypanosomal activity of *Moringa
  oleifera* leaf extract in rats. *GSC Biol. Pharm. Sci.* 11(1): 12–17.
- Gitonga, P.K. et al. (2017). Differential virulence of *T. congolense*
  and *T. brucei* strains. *Onderstepoort J. Vet. Res.* 84(1): a1412.
- Mann, H.B. & Whitney, D.R. (1947). *Ann. Math. Statist.* 18(1): 50–60.
- Mbaya, A.W. et al. (2010). Toxicity and antitrypanosomal activity of *A.
  indica* stem bark. *J. Ethnopharmacol.* 128: 495–400.
- Naessens, J. et al. (2005). *Clin. Exp. Immunol.* 139: 449–456.
- Ngure, R.M. et al. (2009). Antitrypanosomal effects of neem extract on
  *T. brucei rhodesiense*-infected mice. *East. J. Med.* 14: 2–9.
- Noyes, H. et al. (2009). Mechanisms controlling anaemia in *T.
  congolense*-infected mice. *PLoS ONE* 4(4): e5170.
- Ogbole, E. et al. (2021). Anti-trypanosomal activity and phytochemical
  screening of selected *Acacia* species. *J. Chem. Soc. Nigeria* 46(2).
- Okoh, M.E. (2018). In-vivo anti-trypanosomal activity of aqueous *A.
  indica* leaves on *T. brucei brucei*-infected mice. *Int. J. Infect. Dis.
  Ther.* 3(1).
- Rabe, Y. et al. (2024). In-vitro study of *A. indica*, *M. oleifera* and
  *C.ymbopogon* leaf extracts against diminazene-resistant *T. brucei
  brucei*. *SAJOLS*.
- Sirak, B. et al. (2024). In-vitro and in-vivo antitrypanosomal activity
  of *Ranunculus multifidus* and anemonin against *T. congolense*. *BMC
  Vet. Res.* 20: 38 (article 03856).
- Spearman, C. (1904). *Am. J. Psychol.* 15(1): 72–85.
- Tauheed, A. et al. (2021). Partially purified *A. indica* leaf fractions
  inhibit trypanosome alternative oxidase. *PMC* 8217781.
- Ujah, F.O. et al. (2020). In-vitro and in-vivo effect of *M. oleifera*
  seed extract on *T. brucei* and *T. congolense* in rats. *Int. J. Green
  Herb. Chem.* 9(2).
- Wanzala, E.N. et al. (2017). In-vitro and in-vivo anti-trypanosomal
  activities of methanol extract of *A. indica* stem bark. *Afr. J. Trad.
  Complement. Altern. Med.* 14(6).
- Wilcoxon, F. (1945). *Biometrics* 1(1): 86–89.
